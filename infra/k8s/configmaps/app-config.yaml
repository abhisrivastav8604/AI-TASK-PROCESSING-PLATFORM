apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: ai-task-platform
data:
  NODE_ENV: "production"
  PORT: "5000"
  CORS_ORIGIN: "https://ai-task-platform.local" # Adjust based on actual domain
  LOG_LEVEL: "info"
